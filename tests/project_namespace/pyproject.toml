[project]
name = "virtual1.virtual2.project_namespace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv.sources]
tinybuild = { path = "../..", editable = true }

[build-system]
requires = ["tinybuild"]
build-backend = "tinybuild"
