[project]
name = "namespace1.namespace2.project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv.sources]
tinybuild = { path = "../..", editable = true }

[build-system]
requires = ["tinybuild"]
build-backend = "tinybuild"
